# Required in production. Use a long random value.
JWT_SECRET=replace-with-a-long-random-secret

# Optional. Vercel serves the frontend and API from the same domain, so this is normally not needed.
# VITE_API_URL=/api
